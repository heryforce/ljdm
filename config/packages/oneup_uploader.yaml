oneup_uploader:
  mappings:
    photos:
      frontend: dropzone
      namer: app.photo_namer
      storage:
        directory: "%kernel.project_dir%/public/photos/plantes/"

    carousel:
      frontend: dropzone
      namer: app.photo_namer
      storage:
        directory: "%kernel.project_dir%/public/photos/carousel/"
