{% extends 'base.html.twig' %}

{% block title %}Accueil
{% endblock %}

{% block body %}
	<h1 class="text-center mb-4">Le jardin de Melody</h1>

	<div class="row">
		<div class="col">
			<div class="row h-25">
				<div class="col text-center">
					<a class="btn" href="{{ path('home') }}">Accueil</a>
				</div>
				<div class="col text-center">
					<a class="btn" href="#">Les conseils de Melo</a>
				</div>
			</div>
			<div class="row h-25">
				<div class="col text-center">
					<a class="btn" href="{{ path('chez_melo') }}">Chez Melo</a>
				</div>
				<div class="col text-center">
					<a class="btn" href="#">Mon jardin</a>
				</div>
			</div>
			<div class="row h-25">
				<div class="col text-center">
					<a class="btn" href="{{ path('proposition_plante') }}">Proposer une plante</a>
				</div>
				{% if is_granted('ROLE_ADMIN') %}
					<div class="col text-center">
						<a class="btn" href="{{ path('dashboard') }}">Administration</a>
					</div>
				{% endif %}
			</div>
		</div>
		<div class="col">
			<img src="{{ asset('photos/app/plantes_accueil.jpg') }}" alt="" class="img-fluid rounded">
		</div>
	</div>

	{# <div id="carouselAccueil" class="carousel slide" data-bs-ride="carousel"> #}
{# <div class="carousel-inner"> #}
	{# {% for photo in carousel %} #}
	{# <div class="carousel-item active"> #}
	{# <img src="{{ asset('photos/carousel/' ~ photo.fichier) }}" alt="" class="d-block w-50"> #}
	{# </div> #}
	{# {% endfor %} #}
	{# </div> #}
	{# <button class="carousel-control-prev" type="button" data-bs-target="#carouselAccueil" data-bs-slide="prev"> #}
	{# <span class="carousel-control-prev-icon" aria-hidden="true"></span> #}
	{# <span class="visually-hidden">Previous</span> #}
	{# </button> #}
	{# <button class="carousel-control-next" type="button" data-bs-target="#carouselAccueil" data-bs-slide="next"> #}
	{# <span class="carousel-control-next-icon" aria-hidden="true"></span> #}
	{# <span class="visually-hidden">Next</span> #}
	{# </button> #}
	{# </div>  #}


{% endblock %}
